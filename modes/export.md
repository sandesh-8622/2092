# Mode: export

All data is already plain text. Checkins are TSV, thoughts are markdown, finances are markdown, notes are markdown, priorities are YAML.

For a full export: zip everything in data/, config/, memory/, and reports/.

They can also ask for specific exports: "give me my check-ins as CSV" or "export my finance log."
